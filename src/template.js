//example
const genTeam = data => {

    const genMan = manager => {
        return `
            <div>
            ${manager.getName()}
            </div>
        `
    },

    // engineer & intern as well
}